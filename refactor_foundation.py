#standard edition is re-installed within the same directory for test purposes

import os
import random
import parakeet
import math
from os import system

virtual_space_details.fetch(random.randint()).
